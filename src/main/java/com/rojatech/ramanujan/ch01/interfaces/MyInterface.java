// By default, all methods in interface is public. no need to add public
// We can create reference of Interface, but we cant create object of interface
// only public and abstract allowed
package com.rojatech.ramanujan.ch01.interfaces;

public interface MyInterface {
	void show(int i);

}
